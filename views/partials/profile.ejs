<div class="profile-header">
    <div class="profile-img" style="background-image: url(<%=  imgurl%>)"></div>
    <div class="profile-name"><%=  title%></div>

    <% for(var i=0; i < genres.length; i++) { %>
        <div class="genre-tag"><%=  genres[i]%></div>
    <% } %>

    <div class="progress-button-container">
        <form id="chat" role="form" method="get" action= "/chat" >
            <a id=<%= "chatButton" + id %> href="#" class="progress-button" data-loading="Chat" data-finished="Chat">Chat</a>
        </form>
    </div>
</div>
