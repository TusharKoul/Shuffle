<div class="app-title-bar">
	<div class = "row">
		<div class = "col-xs-2">  
			<% if(settings){ %>
			<form id="loginForm" role="form" method="get" action="/chat" >
			<button type = "submit">
        		    <span class="glyphicon glyphicon-cog"></span>
    		</button>
    		</form>
    		<% } %>

		</div>
		<div class = "col-xs-8">
				<h1 class="home-title" style:"font-size: 36px;">Home</h1>
		</div>
		<div class = "col-xs-2">  
			<% if(logoff){ %>
			<form id="loginForm" role="form" method="get" action= "/login" >
			<button type = "submit">
        		    <span class="glyphicon glyphicon-off"></span>
    		</button>
    		</form>
    		<% } %>
    	</div>
	</div>
</div>
